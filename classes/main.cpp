#include "../headers/Menu.h"

int main(int argc, char**argv){
    Menu::getWelcomeMenu();
    exit(0);
    bool run = true;
    while (run != false){
        cout << "hi";
        run = false;
    }
    return 0;
}